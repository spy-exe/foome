import {
  // Tabs
  Home, Compass, ShoppingBag, User,
  // Busca
  Search, Filter, SlidersHorizontal,
  // Localização
  MapPin, Map, Navigation,
  // Restaurante / pedido
  Star, Clock, Truck, ChefHat,
  // Perfil
  Heart, Bell, Settings, HelpCircle,
  // Ações
  Plus, Minus, Trash2, Pencil,
  // Navegação
  Check, X, ChevronRight, ChevronLeft,
  ArrowLeft, RefreshCw, LogOut,
  // Pagamento
  Tag, CreditCard, Wallet, QrCode,
  // Status
  Package, CheckCircle2, AlertCircle,
} from 'lucide-react-native';

export {
  Home, Compass, ShoppingBag, User,
  Search, Filter, SlidersHorizontal,
  MapPin, Map, Navigation,
  Star, Clock, Truck, ChefHat,
  Heart, Bell, Settings, HelpCircle,
  Plus, Minus, Trash2, Pencil,
  Check, X, ChevronRight, ChevronLeft,
  ArrowLeft, RefreshCw, LogOut,
  Tag, CreditCard, Wallet, QrCode,
  Package, CheckCircle2, AlertCircle,
};

export const ICON_SIZE = { xs: 14, sm: 16, md: 20, lg: 24, xl: 32 };
export const ICON_COLOR_DEFAULT = '#0A0A0A';
